# 測試計畫：程式碼品質改進 階段 4＋5（風格統一 + 響應式）

> 測試方法定義請見 [../testing.md](../testing.md)；使用案例與流程圖請見
> [../system_design.md](../system_design.md)；資料細節請見 [../DD.md](../DD.md)。

## 基本資訊

- 日期：2026-08-23
- 測試人員：（開發者本人）
- 變更／功能範圍：[refactor-prompt.md](../refactor-prompt.md) 階段 4、5。
  **全部是視覺與版面改動，沒有任何邏輯或資料流變更。**
  階段 4：補完 17 處 `dynamic` 型別、20 處裸 `fontSize` → `textTheme`、29 處裸間距 →
  `AppSpacing`、13 處裸 `Colors` → `AppColors`、9 個 future 包 `unawaited()`、8 處 `const`。
  階段 5：新增 `widgets/responsive_body.dart`，12 個單欄畫面統一套用（其中 8 個是新增響應式）。
- 對應章節／使用案例：§3-F、§5-E

## 採用的測試類型

- [x] 系統測試
- [x] 迴歸測試
- [x] 跨裝置測試
- [x] 邊界測試
- [ ] 單元測試（既有 40 案例作為迴歸基準，本階段未新增）

## 自動化測試

| # | 指令 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|
| A1 | `cd src && flutter test` | 40 個案例全綠 | 40/40 通過 | ☑ 是 ☐ 否 |
| A2 | `cd src && flutter analyze` | 僅剩 2 個既有的 `onReorder` deprecation | 2 issues，零 error 零 warning（階段 1 加的 5 條 lint 規則全部歸零） | ☑ 是 ☐ 否 |

⚠️ **自動化完全驗證不到本階段的改動**——改的是字距、權重、顏色、版面寬度，
`flutter test` 對這些一無所知。下面的手動案例是唯一的驗證。

---

## 階段 4：字級與顏色（**會看到差異，不是無損替換**）

### 字距與權重

| # | 類型 | 操作步驟 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|---|
| 1 | 跨裝置 | ⚠️ **實機**看今日頁任務卡片（挑一個標題長、又有循環規則與連結的） | 文字比改版前略寬（字距 0.25→0.40），但**不出現黃黑條紋的 RIGHT OVERFLOWED** | | ☐ 是 ☐ 否 |
| 2 | 邊界 | 建一個「每月多選 1、15、最後一天」＋高優先度的任務 | 卡片文字以 … 截斷，不溢出 | | ☐ 是 ☐ 否 |
| 3 | 系統 | 今日頁週視圖，看星期表頭（一二三四五六日） | 字比改版前**略粗**（w400→w500，改用 `labelSmall`），排版沒跑掉 | | ☐ 是 ☐ 否 |
| 4 | 系統 | 任務卡片的優先度徽章（高／中） | 同樣略粗，徽章寬度沒被撐破 | | ☐ 是 ☐ 否 |
| 5 | 系統 | 學期目標詳情頁的分類 chip | 同樣略粗，chip 沒被撐破 | | ☐ 是 ☐ 否 |
| 6 | 系統 | 學期目標詳情頁「連結的願景」欄位標題 | 同樣略粗 | | ☐ 是 ☐ 否 |
| 7 | 迴歸 | 願景詳情頁的子願景標題、分類 chip、連結目標副標 | 字級與改版前一致（14/12/12 都有對應角色，只有字距變） | | ☐ 是 ☐ 否 |
| 8 | 迴歸 | 「我的」頁與日記列表的「Day N」徽章 | 外觀不變（原本就有指定 w600，會覆蓋 `labelSmall` 的 w500） | | ☐ 是 ☐ 否 |
| 9 | 迴歸 | 日記編輯頁的內文輸入框 | 字級仍是 16、行高 1.7 | | ☐ 是 ☐ 否 |
| 10 | 迴歸 | 設定頁「開發者模式」小標（需連點版本 7 次解鎖） | 仍是粗體、有字距 0.8 | | ☐ 是 ☐ 否 |
| 11 | 迴歸 | 今日頁的篩選徽章「篩選 · N」 | 仍是 w600、主色 | | ☐ 是 ☐ 否 |

### 刻意保留 inline 的 3 處（不該有變化）

| # | 類型 | 操作步驟 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|---|
| 12 | 迴歸 | 今日頁週視圖的日期數字（`3/15` 這種） | **完全沒變**（15px，型別量表沒有對應角色，保留 inline） | | ☐ 是 ☐ 否 |
| 13 | 迴歸 | 關聯圖的節點副標與任務數徽章 | **完全沒變**（10.5px／10px，配合節點框大小） | | ☐ 是 ☐ 否 |

### 顏色

| # | 類型 | 操作步驟 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|---|
| 14 | 系統 | 登入頁的「或」分隔文字 | 從中性灰 `#9E9E9E` 變**暖褐灰** `#B09A84`，與周圍色調一致 | | ☐ 是 ☐ 否 |
| 15 | 系統 | 設定頁的匿名回饋說明文字 | 同上 | | ☐ 是 ☐ 否 |
| 16 | 迴歸 | 首頁四個 FAB 的圖示、頭像的首字母、按鈕上的載入圈 | **完全沒變**（`Colors.white` 與 `AppColors.textOnPrimary` 都是 `#FFFFFFFF`，純改名） | | ☐ 是 ☐ 否 |
| 17 | 迴歸 | 願景頁清單頂端的漸層遮罩 | **完全沒變**（漸層裡的白是字面上的白，刻意沒改） | | ☐ 是 ☐ 否 |

### 間距

| # | 類型 | 操作步驟 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|---|
| 18 | 迴歸 | 隨意走過 5 個畫面 | 間距**完全沒變**（只把 `8`/`4`/`16` 換成 `AppSpacing.sm`/`xs`/`md`，數值相同） | | ☐ 是 ☐ 否 |

### 型別與非同步（無視覺變化，測不壞為準）

| # | 類型 | 操作步驟 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|---|
| 19 | 迴歸 | 設定頁：切語言、切日期格式、切預設任務視角、改學期制 | 四個對話框都正常（這四個 helper 原本用 `dynamic s`） | | ☐ 是 ☐ 否 |
| 20 | 迴歸 | 「我的」→ 帳號設定，改使用者名稱／學校／系所／年級／頭像後儲存 | 全部正常（`_EditProfileDialog` 原本三個欄位是 `dynamic`） | | ☐ 是 ☐ 否 |
| 21 | 迴歸 | 回收桶「清空」對話框 | 正常跳出 | | ☐ 是 ☐ 否 |
| 22 | 系統 | **登出再登入** | 八個 provider 仍並行載入完成，資料齊全（改用 `unawaited()` 只是標註意圖，行為不變） | | ☐ 是 ☐ 否 |

---

## 階段 5：響應式

### 8 個新套用的畫面

桌面瀏覽器把視窗**從 400px 慢慢拉到 1920px**，逐一確認：768px 之前鋪滿、之後置中限寬、
切換瞬間不閃爍、內容不重複載入。

| # | 類型 | 畫面 | maxWidth | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|---|---|
| 23 | 跨裝置 | 註冊頁 | 420 | 與登入頁**寬度一致**（先前只有登入頁有響應式，這一對特別明顯） | | ☐ 是 ☐ 否 |
| 24 | 跨裝置 | 個人資料設定頁（新帳號首次登入） | 420 | 頭像九宮格置中不被拉伸 | | ☐ 是 ☐ 否 |
| 25 | 跨裝置 | 設定 → 分類設定 | 640 | 清單置中；**底部新增列仍在畫面內、可輸入** | | ☐ 是 ☐ 否 |
| 26 | 跨裝置 | 「我的」→ 所有日記 | 640 | 卡片置中；FAB 仍在右下角（在 `ResponsiveBody` 外） | | ☐ 是 ☐ 否 |
| 27 | 跨裝置 | 日記編輯頁 | 640 | 上方送出列、下方字數列都置中對齊內容 | | ☐ 是 ☐ 否 |
| 28 | 跨裝置 | 完成度歷史 | 640 | ⚠️ **長條圖仍可水平捲動、寬度計算沒壞**（內部 CustomPainter 有自己的寬度邏輯） | | ☐ 是 ☐ 否 |
| 29 | 跨裝置 | 設定 → 回收桶（有資料時） | 640 | 清單置中 | | ☐ 是 ☐ 否 |
| 30 | 邊界 | 回收桶（**空的時候**） | 640 | 「垃圾桶是空的」仍置中顯示（這是三元運算式的另一分支） | | ☐ 是 ☐ 否 |
| 31 | 跨裝置 | 「我的」→ 所有靈感 | 640 | 清單置中 | | ☐ 是 ☐ 否 |
| 32 | 邊界 | 所有日記（**空的時候**） | 640 | 「尚無日記」仍置中顯示 | | ☐ 是 ☐ 否 |

### 4 個既有畫面改用 `ResponsiveBody`（**不該有任何變化**）

| # | 類型 | 畫面 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|---|
| 33 | 迴歸 | 登入頁 | 桌面寬度下與改版前**完全相同**（420 置中） | | ☐ 是 ☐ 否 |
| 34 | 迴歸 | 設定頁 | 同上（640 置中） | | ☐ 是 ☐ 否 |
| 35 | 迴歸 | 學期目標詳情頁 | 同上（640 置中） | | ☐ 是 ☐ 否 |
| 36 | 迴歸 | 願景詳情頁 | 同上（640 置中） | | ☐ 是 ☐ 否 |

### 四個分頁（**不使用** `ResponsiveBody`，不該被影響）

| # | 類型 | 操作步驟 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|---|
| 37 | 迴歸 | 今日／學期／未來／我的，各拉一次寬度 | 仍是雙欄 + NavigationRail，`isWide ? 1100 : 900` 未受影響 | | ☐ 是 ☐ 否 |
| 38 | 邊界 | 視窗寬度停在 **767px 與 768px** | 12 個單欄畫面在這個邊界正確切換，沒有半套版面 | | ☐ 是 ☐ 否 |

### 手機實機

| # | 類型 | 操作步驟 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|---|
| 39 | 跨裝置 | 實機走過上述 12 個畫面 | 全部維持鋪滿寬度，**與改版前完全相同**（手機寬度 < 768，`ResponsiveBody` 直接回傳 child） | | ☐ 是 ☐ 否 |
| 40 | 跨裝置 | 實機橫向旋轉，看這 12 個畫面 | 若橫向寬度 ≥ 768 會變成置中限寬——確認可接受 | | ☐ 是 ☐ 否 |

## 發現的問題

（記錄測試中發現的 bug 或行為不符預期之處）

## 結論

- [ ] 全部案例通過，可視為完成
- [ ] 部分未通過，需修正後重測（列出待修項目）

## 備註

1. **案例 1～6 是本次最需要盯的**。字距與權重的變化沒有自動化能攔，而任務卡片上一批才剛修好
   溢出問題。
2. `SizedBox` 的 `12`（12 處）與 `2`（12 處）**沒有**對應的 `AppSpacing` 常數——現有級距是
   4/8/16，跳過了 12。刻意沒有自創新常數，那是設計決定不是重構決定。
3. `docs/test-plans/TEMPLATE.md` 仍列著舊的 10 種測試類型，`testing.md` 已縮成 5 種。
   兩者不同步，與本次改動無關。
