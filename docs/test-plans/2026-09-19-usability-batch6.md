# 測試計畫：使用回饋第六批（循環任務歸屬、完成動畫、日期、通知、移除子任務與任務→願景）

> 測試方法定義請見 [../testing.md](../testing.md)；使用案例與流程圖請見
> [../system_design.md](../system_design.md)（§2-B、§2-D、§3-A、§3-J、§3-K、§3-L、UC3-B、UC7）；
> 資料細節請見 [../DD.md](../DD.md)。

## 基本資訊

- 日期：2026-09-19
- 測試人員：（開發者本人）
- 變更／功能範圍：
  1. **修 bug**：循環任務在「全部任務」不看規則、每天都出現 → `currentOccurrence()` 決定每一列代表哪一次
  2. **修 bug**：完成動畫看不到了（勾選當下該列就離開清單）→ 改畫在 `Overlay` 上
  3. 預設頭像 10 → **24 個**（前十個位置不動）
  4. **移除子任務**功能（`parent_task_id` 欄位保留不用）
  5. 篩選目標的對話框改成**依學期分組**的複選（與連結挑選器共用同一套分組）
  6. 任務／目標／願景標題**兩行**時的排版（頂端對齊、卡片放寬到 2 行）
  7. **移除「任務→願景」**連結（保留任務→目標、目標→願景；`linked_goal_id` 欄位保留不用）
  8. **修 bug**：當天的日期有機會是錯的（`effectiveNowProvider` 被 `Provider` 快取）
  9. **修 bug**：通知點進 App 就消失 → 改成留到該任務完成才收掉
  10. 「我的」頁改版：A 的分區結構 + B 的三張摘要卡（連續寫日記天數／靈感數／本學期目標數）
- 對應章節：§2-B、§2-D、§3-A（新增循環歸屬與換日）、§3-J、§3-K（通知留存）、§3-M、UC3、UC3-B、UC6、UC7

## 採用的測試類型

- [x] 單元測試（`current_occurrence_test` 11、`date_rollover_test` 3、`me_stats_test` 4、`category_palette_test` +1、`notification_persistence_source_test` 3）
- [x] 系統測試（widget：`target_filter_dialog_test` 4、`completion_effect_test` +1）
- [x] 迴歸測試（既有案例全綠：278 → 290）
- [x] 邊界測試（規則還沒開始的循環任務、沒有日期的任務、今天還沒寫日記、沒有目標時的篩選對話框）
- [ ] 跨裝置測試（**通知與換日只能實機看**，案例 8 起）

## 自動化測試

| # | 指令 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|
| A1 | `cd src && flutter analyze` | `No issues found!` | No issues found（4.5s） | ☑ 是 |
| A2 | `cd src && flutter test` | 全綠（278 → 290） | 290/290 | ☑ 是 |
| A3 | 反向驗證：`currentOccurrence()` 永遠回今天 | 循環歸屬測試轉紅 | 7 個案例轉紅，還原後全綠 | ☑ 是 |
| A4 | 反向驗證：`apply()` 改回 `cancelAll()` | 通知防呆測試轉紅 | 「只取消尚未觸發的排程」轉紅，還原後全綠 | ☑ 是 |
| A5 | `cd src && flutter build apk --debug` | 建置成功 | 建置成功 | ☑ 是 |

> `notification_service.dart` 走 platform channel，`flutter test` 裡沒有通道可用，
> 所以第 9 項的三個開關由 `notification_persistence_source_test.dart` **讀原始碼**把關
> （與 `row_id_source_test.dart` 同一套做法）。真正的行為仍只能靠下方的實機案例 8–11。

## 測試案例（實機／模擬器）

| # | 測試類型 | 輸入／操作步驟 | 預期結果 | 實際結果 | 通過？ |
|---|---|---|---|---|---|
| 1 | 系統 | 建一個「每月 20 號」的循環任務，在非 20 號的日子開「全部任務」 | 該任務出現在未完成區，副標寫的是**最近一次 20 號**，不是今天 | | ☐ 是 ☐ 否 |
| 2 | 系統 | 承上，勾掉它 | 立刻進「已完成」區；切回「本日任務」該任務不出現 | | ☐ 是 ☐ 否 |
| 3 | 邊界 | 承上，等到下一個 20 號（或改系統時間） | 該任務回到未完成區 | | ☐ 是 ☐ 否 |
| 4 | 系統 | 在今日頁與每週檢視各勾一筆任務 | 看得到放大再縮回的動畫，且該列離開清單後動畫仍播完，畫面沒有殘留方框 | | ☐ 是 ☐ 否 |
| 5 | 系統 | 「我的」頁 → 換頭像 | 24 個頭像可選；原本選的頭像沒有被換掉 | | ☐ 是 ☐ 否 |
| 6 | 系統 | 開新增任務表單、長按任務列 | **沒有**「新增子任務」入口；拖到別的任務列身上只會換順序，不會縮排 | | ☐ 是 ☐ 否 |
| 7 | 系統 | 新增任務表單、任務卡片、關聯圖、願景詳情頁 | 都**沒有**任務連結願景的入口與顯示；目標仍可連願景 | | ☐ 是 ☐ 否 |
| 8 | 系統 | 今日頁點篩選 | 對話框上方一排學期 chip、停在當前學期；勾選可複選、勾了不關閉；有「重置」 | | ☐ 是 ☐ 否 |
| 9 | 邊界 | 一個字很長的任務／目標／願景標題 | 最多兩行、超出才省略；勾選框與右側按鈕仍對齊第一行 | | ☐ 是 ☐ 否 |
| 10 | 跨裝置 | App 開著放過午夜（或改系統時間再回前景） | 日期換成新的一天；若使用者自己翻到別的日期則不被搶走 | | ☐ 是 ☐ 否 |
| 11 | 跨裝置 | 讓任務通知跳出 → 點通知進 App | 通知**還在**通知欄 | | ☐ 是 ☐ 否 |
| 12 | 跨裝置 | 承上，在 App 內把該任務勾完成 | 通知消失 | | ☐ 是 ☐ 否 |
| 13 | 邊界 | 承上，改勾**別的**任務、或新增一筆任務（觸發排程重算） | 原本那則通知**不受影響**，仍留在通知欄 | | ☐ 是 ☐ 否 |
| 14 | 系統 | 「我的」頁 | 個人資料卡下方三張摘要卡：連續寫日記天數、靈感數、本學期目標數，數字與實際資料相符 | | ☐ 是 ☐ 否 |

## 發現的問題

（待實機測試後填寫）

## 結論

- [ ] 全部案例通過，可視為完成
- [ ] 部分未通過，需修正後重測（列出待修項目）
