class AppBreakpoints {
  // Desktop layout starts here; below this width is the mobile layout
  static const desktop = 768.0;
  // Wide desktop gets larger content caps and the extended navigation rail
  static const wide = 1200.0;
}
