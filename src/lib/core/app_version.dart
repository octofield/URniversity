// The app's version, in one place. The settings screen shows it and
// pubspec.yaml's `version:` field carries the same numbers for the store build.
//
// How it moves (2026-09-20):
//  - patch (0.0.X): a batch of fixes, copy or layout only
//  - minor (0.X.0): a batch that adds or removes a feature
//  - major (X.0.0): a change to how data is stored or to a core flow
//
// The `alpha-` prefix comes off only when the user says so — never as part of
// a feature batch.
const String kAppVersion = 'alpha-1.0.0';
