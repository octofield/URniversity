// Symbols reused across screens. They carry nothing to translate, but they
// repeat often enough that a stray variant would be hard to spot by eye

// Shown where a value is not set yet
const String kEmptyValue = '—';

// Points from an item to what it links to
const String kArrow = '→';

// Joins two short facts on one line
const String kDotSeparator = ' · ';
